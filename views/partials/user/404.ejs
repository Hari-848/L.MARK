<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>404 - Page Not Found</title>
    <!-- Try an alternative CDN -->
    <link href="https://unpkg.com/tailwindcss@^2/dist/tailwind.min.css" rel="stylesheet">
    <!-- Fallback inline styles in case Tailwind fails to load -->
    <style>
        body { font-family: sans-serif; background-color: #f3f4f6; display: flex; align-items: center; justify-content: center; min-height: 100vh; margin: 0; }
        .container { text-align: center; max-width: 32rem; padding: 1.5rem; background-color: white; border-radius: 0.5rem; box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1); }
        h1 { font-size: 3.75rem; font-weight: bold; color: #dc2626; margin: 0; }
        h2 { font-size: 1.5rem; font-weight: 600; color: #1f2937; margin-top: 1rem; }
        p { color: #4b5563; margin-top: 1rem; }
        .home-link { display: inline-block; margin-top: 1.5rem; padding: 0.75rem 1.5rem; color: white; background-color: #27272a; border-radius: 0.375rem; text-decoration: none; font-weight: bold; }
        .home-link:hover { background-color: #3f3f46; }
    </style>
</head>
<body class="bg-gray-100 flex items-center justify-center min-h-screen">
    <div class="text-center max-w-lg p-6 bg-white rounded-lg shadow-md">
        <h1 class="text-6xl font-bold text-red-600">404</h1>
        <h2 class="text-2xl font-semibold text-gray-800 mt-4">Page Not Found</h2>
        <p class="text-gray-600 mt-4">
            Sorry, the page you're looking for doesn't exist. It may have been moved or deleted.
        </p>
        <a href="<%= locals.isAdmin ? '/admin/dashboard' : '/' %>" class="home-link mt-6 inline-block px-6 py-3 text-white bg-blue-600 rounded-md shadow hover:bg-blue-700 focus:ring-2 focus:ring-offset-2 focus:ring-blue-500">
            Back to Homepage
        </a>
 
    </div>
</body>
</html>
